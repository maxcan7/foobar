# Pre-set variables e.g. default paths
import os


groups_definition = os.path.join(
    os.getcwd(), "groups_definitions", "userGroupPercentages.json"
)
users_path = os.path.join(os.getcwd(), "users_paths", "usersDefault.csv")
